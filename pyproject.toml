[build-system]
requires = ["setuptools>=68,<76", "wheel", "Cython>=3.0", "numpy>=1.14"]
build-backend = "setuptools.build_meta"

[project]
name = "mdfreader"
version = "4.2"

[tool.setuptools]
license-files = []
